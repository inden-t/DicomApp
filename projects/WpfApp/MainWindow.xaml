<Window x:Class="DICOMViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DICOMViewer"
        xmlns:global="clr-namespace:"
        xmlns:viewmodels="clr-namespace:DICOMViewer.ViewModels"
        xmlns:views="clr-namespace:DICOMViewer.Views"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="DICOM Viewer" Height="450" Width="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Menu Grid.Row="0">
      <MenuItem Header="_File">
        <MenuItem Header="_Open" Command="{Binding OpenDICOMFileCommand}"/>
        <Separator/>
        <MenuItem Header="_Exit" Command="{Binding ExitCommand}"/>
      </MenuItem>
      <MenuItem Header="_View">
        <MenuItem Header="_Zoom In" Command="{Binding ZoomInCommand}"/>
        <MenuItem Header="_Zoom Out" Command="{Binding ZoomOutCommand}"/>
        <Separator/>
        <MenuItem Header="_Pan" Command="{Binding PanCommand}"/>
        <Separator/>
        <MenuItem Header="_Rotate" Command="{Binding RotateCommand}"/>
      </MenuItem>
    </Menu>

    <ContentControl x:Name="ImageContainer" Grid.Row="1" />
  </Grid>
</Window>
