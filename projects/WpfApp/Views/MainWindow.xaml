<Window x:Class="DicomApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:DicomApp.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="DICOM Viewer"
        Height="900"
        Width="1200">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="200"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Menu Grid.Column="0"
          Grid.ColumnSpan="2"
          Grid.Row="0">
      <MenuItem Header="_File">
        <MenuItem Header="_Open File"
                  Command="{Binding OpenDicomFileCommand}"/>
        <MenuItem Header="Open _Folder"
                  Command="{Binding OpenDicomFolderCommand}"/>
        <Separator/>
        <MenuItem Header="_Exit"
                  Command="{Binding ExitCommand}"/>
      </MenuItem>
      <MenuItem Header="_View">
        <MenuItem Header="_Zoom In"
                  Command="{Binding ZoomInCommand}"/>
        <MenuItem Header="_Zoom Out"
                  Command="{Binding ZoomOutCommand}"/>
        <Separator/>
        <MenuItem Header="_Pan"
                  Command="{Binding PanCommand}"/>
        <Separator/>
        <MenuItem Header="_Rotate"
                  Command="{Binding RotateCommand}"/>
      </MenuItem>
      <MenuItem Header="_Zoom In"
                Command="{Binding ZoomInCommand}"/>
      <MenuItem Header="_Zoom Out"
                Command="{Binding ZoomOutCommand}"/>
      <MenuItem Header="_Make 3D Blood Vessel"
                Command="{Binding MakeBloodVesselPointCloud3DCommand}"/>
    </Menu>

    <ListView Grid.Column="0"
              Grid.Row="1"
              ItemsSource="{Binding DicomFiles}"
              SelectedIndex="{Binding SelectedIndex.Value, Mode=TwoWay}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextBlock Text="{Binding}"/>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>

    <ContentControl x:Name="ImageContainer"
                    Grid.Column="1"
                    Grid.Row="1"/>
  </Grid>
</Window>
