<UserControl x:Class="DICOMViewer.Views.ImageViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:DICOMViewer.ViewModels"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:interactivity="clr-namespace:Reactive.Bindings.Interactivity;assembly=ReactiveProperty.WPF"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:ImageViewerViewModel}"
            >
  <Grid>
    <Image x:Name="DicomImage"
             Stretch="Uniform"
             Source="{Binding BitmapSourceImage.Value}"/>
    <Canvas x:Name="CanvasOverlay"/>
    <b:Interaction.Triggers>
      <b:EventTrigger EventName="MouseMove">
        <interactivity:EventToReactiveProperty ReactiveProperty="{Binding MouseMove}" />
      </b:EventTrigger>
    </b:Interaction.Triggers>
  </Grid>
</UserControl>
